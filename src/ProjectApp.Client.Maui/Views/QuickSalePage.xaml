<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:ProjectApp.Client.Maui.Converters"
             x:Class="ProjectApp.Client.Maui.Views.QuickSalePage"
             x:Name="RootPage"
             Title="Быстрая продажа">
  <ContentPage.Resources>
    <ResourceDictionary>
      <converters:PaymentTypeToRuConverter x:Key="PaymentTypeToRuConverter"/>
    </ResourceDictionary>
  </ContentPage.Resources>
  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Настройки" Clicked="OnSettingsClicked" />
  </ContentPage.ToolbarItems>
  <ContentPage.Content>
    <Grid RowDefinitions="Auto,Auto,*,Auto,Auto,Auto,Auto" Padding="12">
      <!-- Offline banner -->
      <Frame Grid.Row="0" BackgroundColor="#FFCC00" Padding="8" HasShadow="False" IsVisible="{Binding IsOffline}">
        <HorizontalStackLayout Spacing="12">
          <Label Text="Оффлайн режим (моки)" FontAttributes="Bold" VerticalOptions="Center"/>
          <Button Text="Повторить" Command="{Binding RetryCommand}" />
        </HorizontalStackLayout>
      </Frame>

      <!-- Search -->
      <Grid Grid.Row="1" ColumnDefinitions="*,Auto,200" ColumnSpacing="8">
        <HorizontalStackLayout Grid.Column="0" Spacing="8">
          <Label Text="Поиск:" VerticalOptions="Center"/>
          <Entry Placeholder="SKU или название" Text="{Binding Query}" HorizontalOptions="FillAndExpand"/>
        </HorizontalStackLayout>
        <Label Grid.Column="1" Text="Категория:" VerticalTextAlignment="Center"/>
        <Picker Grid.Column="2" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"/>
      </Grid>

      <!-- Results & Cart side by side -->
      <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="12">
        <!-- Search results as cards grid -->
        <VerticalStackLayout Grid.Column="0">
          <Label Text="Результаты" FontAttributes="Bold"/>
          <CollectionView ItemsSource="{Binding SearchResults}" Margin="0,8,0,0">
            <CollectionView.ItemsLayout>
              <GridItemsLayout Orientation="Vertical" Span="4" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
              <DataTemplate>
                <Frame Padding="8" HasShadow="False" CornerRadius="10" HeightRequest="140">
                  <Grid RowDefinitions="Auto,*,Auto" ColumnDefinitions="*">
                    <Label Grid.Row="0" Text="{Binding Name}" FontAttributes="Bold" LineBreakMode="TailTruncation"/>
                    <VerticalStackLayout Grid.Row="1" Spacing="4">
                      <Label Text="{Binding Sku}" FontSize="12" TextColor="Gray"/>
                      <Label Text="{Binding Price, StringFormat='{0:C}'}" FontSize="14"/>
                      <HorizontalStackLayout Spacing="10">
                        <Label Text="{Binding Nd40Qty, StringFormat='ND-40 {0}'}" FontSize="11"/>
                        <Label Text="{Binding Im40Qty, StringFormat='IM-40 {0}'}" FontSize="11"/>
                        <Label Text="{Binding TotalQty, StringFormat='Всего {0}'}" FontSize="11"/>
                      </HorizontalStackLayout>
                    </VerticalStackLayout>
                    <Button Grid.Row="2" Text="Добавить" Command="{Binding Source={x:Reference RootPage}, Path=BindingContext.AddToCartCommand}" CommandParameter="{Binding .}"/>
                  </Grid>
                </Frame>
              </DataTemplate>
            </CollectionView.ItemTemplate>
          </CollectionView>
        </VerticalStackLayout>

        <!-- Cart -->
        <VerticalStackLayout Grid.Column="1">
          <Label Text="Корзина" FontAttributes="Bold"/>
          <CollectionView ItemsSource="{Binding Cart}" Margin="0,8,0,0">
            <CollectionView.ItemTemplate>
              <DataTemplate>
                <Grid ColumnDefinitions="*,Auto,Auto,Auto" Padding="4" ColumnSpacing="8">
                  <VerticalStackLayout>
                    <Label Text="{Binding Name}" FontAttributes="Bold"/>
                    <HorizontalStackLayout Spacing="6">
                      <Label Text="Цена:" FontSize="12"/>
                      <Entry Text="{Binding UnitPrice, Mode=TwoWay}" Keyboard="Numeric" WidthRequest="100" FontSize="12"/>
                    </HorizontalStackLayout>
                    <Label Text="{Binding Source={x:Reference RootPage}, Path=BindingContext.Total, StringFormat=''}" IsVisible="False"/>
                  </VerticalStackLayout>
                  <Stepper Grid.Column="1" Minimum="0" Maximum="1000" Increment="1" Value="{Binding Qty, Mode=TwoWay}"/>
                  <Label Grid.Column="2" Text="{Binding Qty, StringFormat='x{0:F0}'}" VerticalTextAlignment="Center"/>
                  <Button Grid.Column="3" Text="Удалить" Command="{Binding Source={x:Reference RootPage}, Path=BindingContext.RemoveFromCartCommand}" CommandParameter="{Binding .}"/>
                </Grid>
              </DataTemplate>
            </CollectionView.ItemTemplate>
          </CollectionView>
        </VerticalStackLayout>
      </Grid>

      <!-- Payment type (locked; change by going back) -->
      <HorizontalStackLayout Grid.Row="3" Spacing="12" Margin="0,12,0,0">
        <Label Text="Тип продажи:" VerticalOptions="Center"/>
        <Label VerticalOptions="Center"
               Text="{Binding SelectedPaymentType, Converter={StaticResource PaymentTypeToRuConverter}}"/>
        <Button Text="Изменить" Command="{Binding ChangePaymentTypeCommand}"/>
        <Entry Placeholder="Имя клиента (необязательно)" Text="{Binding ClientName}" HorizontalOptions="FillAndExpand"/>
        <Button Text="Клиент" Clicked="OnPickClientClicked"/>
      </HorizontalStackLayout>

      <!-- Totals -->
      <HorizontalStackLayout Grid.Row="4" Spacing="12">
        <Label Text="Итого:" FontAttributes="Bold"/>
        <Label Text="{Binding Total, StringFormat='{0:C}'}" FontAttributes="Bold"/>
      </HorizontalStackLayout>

      <!-- Submit -->
      <!-- Reservation notes (visible only for Reservation) -->
      <VerticalStackLayout Grid.Row="5" Spacing="8" IsVisible="{Binding IsReservation}">
        <Label Text="Заметки к броне" FontAttributes="Bold"/>
        <HorizontalStackLayout Spacing="8">
          <Entry Placeholder="Текст заметки" Text="{Binding NewReservationNote}" HorizontalOptions="FillAndExpand"/>
          <Button Text="Добавить" Command="{Binding AddReservationNoteCommand}"/>
        </HorizontalStackLayout>
        <CollectionView ItemsSource="{Binding ReservationNotes}" Margin="0,4,0,0">
          <CollectionView.ItemTemplate>
            <DataTemplate>
              <Grid ColumnDefinitions="*,Auto" Padding="4" ColumnSpacing="8">
                <Label Text="{Binding .}"/>
                <Button Grid.Column="1" Text="Удалить" Command="{Binding Source={x:Reference RootPage}, Path=BindingContext.RemoveReservationNoteCommand}" CommandParameter="{Binding .}"/>
              </Grid>
            </DataTemplate>
          </CollectionView.ItemTemplate>
        </CollectionView>
      </VerticalStackLayout>

      <Button Grid.Row="6" Text="Провести" Command="{Binding SubmitCommand}" Margin="0,8,0,0"/>
    </Grid>
  </ContentPage.Content>
</ContentPage>
