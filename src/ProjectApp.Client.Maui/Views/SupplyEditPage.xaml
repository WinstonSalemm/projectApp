<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ProjectApp.Client.Maui.ViewModels"
             xmlns:services="clr-namespace:ProjectApp.Client.Maui.Services"
             x:Class="ProjectApp.Client.Maui.Views.SupplyEditPage"
             Title="Редактирование поставки"
             x:DataType="{x:Null}">
    <!-- ===== RESOURCES / STYLES ===== -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Табличные стили -->
            <Style x:Key="TableHeaderLabel"
                   TargetType="Label">
                <Setter Property="FontAttributes"
                        Value="Bold" />
                <Setter Property="FontSize"
                        Value="12" />
                <Setter Property="Padding"
                        Value="8,6" />
                <Setter Property="BackgroundColor"
                        Value="#ecf0f1" />
                <Setter Property="TextColor"
                        Value="#2c3e50" />
                <Setter Property="LineBreakMode"
                        Value="TailTruncation" />
            </Style>
            <Style x:Key="CellLabel"
                   TargetType="Label">
                <Setter Property="FontSize"
                        Value="12" />
                <Setter Property="Padding"
                        Value="8,6" />
                <Setter Property="LineBreakMode"
                        Value="TailTruncation" />
                <Setter Property="VerticalTextAlignment"
                        Value="Center" />
            </Style>
            <Style x:Key="CellMuted"
                   TargetType="Label"
                   BasedOn="{StaticResource CellLabel}">
                <Setter Property="FontSize"
                        Value="11" />
                <Setter Property="TextColor"
                        Value="Gray" />
            </Style>
            <Style x:Key="CellNum"
                   TargetType="Label"
                   BasedOn="{StaticResource CellLabel}">
                <Setter Property="HorizontalTextAlignment"
                        Value="End" />
            </Style>
            <Style x:Key="CellBorder"
                   TargetType="Border">
                <Setter Property="Stroke"
                        Value="#d1d5db" />
                <Setter Property="StrokeThickness"
                        Value="1" />
            </Style>
            <Style x:Key="FieldLabel"
                   TargetType="Label">
                <Setter Property="VerticalTextAlignment"
                        Value="Center" />
                <Setter Property="FontSize"
                        Value="13" />
            </Style>
            <Style x:Key="FieldEntry"
                   TargetType="Entry">
                <Setter Property="Keyboard"
                        Value="Numeric" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="HeightRequest"
                        Value="36" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <!-- ===== TOOLBAR ===== -->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Добавить товар"
                     Clicked="OnAddProductClicked"
                     IconImageSource="add.png" />
        <ToolbarItem Text="🔄 Пересчитать"
                     Clicked="OnRecalculateClicked" />
    </ContentPage.ToolbarItems>
    <!-- ===== PAGE ===== -->
    <ScrollView>
        <VerticalStackLayout Padding="10"
                             Spacing="10">
            <!-- Заголовок поставки -->
            <Frame BorderColor="LightGray"
                   CornerRadius="8"
                   Padding="12"
                   Margin="0,0,0,4">
                <Grid RowDefinitions="Auto,Auto"
                      ColumnDefinitions="*,Auto"
                      ColumnSpacing="8">
                    <Label Grid.Row="0"
                           Grid.Column="0"
                           Text="{Binding Supply.Code, StringFormat='Поставка: {0}', FallbackValue='Поставка: …'}"
                           FontSize="20"
                           FontAttributes="Bold" />
                    <Label Grid.Row="1"
                           Grid.Column="0"
                           Text="{Binding Supply.RegisterType, StringFormat='Регистр: {0}', FallbackValue='Регистр: …'}"
                           FontSize="13"
                           TextColor="Gray" />
                </Grid>
            </Frame>
            <!-- Список товаров -->
            <Label Text="Товары в поставке:"
                   FontSize="16"
                   FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding SupplyItems}"
                            SelectionMode="None"
                            HeightRequest="240">
                <CollectionView.EmptyView>
                    <Label Text="Нет товаров. Нажмите '+' чтобы добавить."
                           TextColor="Gray"
                           HorizontalTextAlignment="Center"
                           Margin="0,12,0,0" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="services:SupplyItemDto">
                        <Frame Margin="0,6"
                               Padding="10"
                               BorderColor="LightGray">
                            <Grid RowDefinitions="Auto,Auto,Auto"
                                  ColumnDefinitions="*,Auto"
                                  ColumnSpacing="8">
                                <Label Grid.Row="0"
                                       Grid.Column="0"
                                       Text="{Binding Name}"
                                       FontSize="15"
                                       FontAttributes="Bold" />
                                <Button Grid.Row="0"
                                        Grid.Column="1"
                                        Text="🗑️"
                                        Clicked="OnRemoveProductClicked"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#e74c3c"
                                        TextColor="White"
                                        WidthRequest="36"
                                        HeightRequest="36" />
                                <Label Grid.Row="1"
                                       Grid.Column="0"
                                       Grid.ColumnSpan="2"
                                       Text="{Binding Sku, StringFormat='SKU: {0}'}"
                                       FontSize="12"
                                       TextColor="Gray" />
                                <Grid Grid.Row="2"
                                      Grid.Column="0"
                                      Grid.ColumnSpan="2"
                                      ColumnDefinitions="*,*,*"
                                      ColumnSpacing="8"
                                      Margin="0,4,0,0">
                                    <Label Grid.Column="0"
                                           Text="{Binding Quantity, StringFormat='Кол-во: {0}'}"
                                           FontSize="12" />
                                    <Label Grid.Column="1"
                                           Text="{Binding PriceRub, StringFormat='Цена: {0:F2} ₽'}"
                                           FontSize="12" />
                                    <Label Grid.Column="2"
                                           Text="{Binding Weight, StringFormat='Вес: {0:F2} кг'}"
                                           FontSize="12" />
                                </Grid>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <!-- Секция расчёта (новая) -->
            <Frame BackgroundColor="#2563eb" Padding="12" CornerRadius="8">
                <Label Text="💰 РАСЧЁТ СЕБЕСТОИМОСТИ" TextColor="White" FontSize="18" FontAttributes="Bold" HorizontalTextAlignment="Center" />
            </Frame>
            <ContentView x:Name="CostingSection">
                <ContentView.Content>
                    <VerticalStackLayout Spacing="10">
                        <!-- Excel-like parameter grid -->
                        <Grid ColumnDefinitions="180,180,180,180,180"
                              RowDefinitions="Auto,Auto,Auto"
                              ColumnSpacing="0" RowSpacing="0">
                            <!-- Row 0 -->
                            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="RUB→UZS" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding RubToUzs, Mode=TwoWay}" Placeholder="156"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="USD→UZS" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding UsdToUzs, Mode=TwoWay}" Placeholder="12800"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Таможня (UZS)" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding CustomsFixedUzs, Mode=TwoWay}" Placeholder="0"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="3" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Погрузка (UZS)" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding LoadingTotalUzs, Mode=TwoWay}" Placeholder="0"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="4" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Наценка" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding TradeMarkupPct, Mode=TwoWay}" Placeholder="0.15 = 15%"/>
                                </VerticalStackLayout>
                            </Border>

                            <!-- Row 1 -->
                            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Логистика" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding LogisticsPct, Mode=TwoWay}" Placeholder="0.05 = 5%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Склад" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding WarehousePct, Mode=TwoWay}" Placeholder="0.005 = 0.5%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Декл" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding DeclarationPct, Mode=TwoWay}" Placeholder="0.002 = 0.2%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="3" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Сертиф" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding CertificationPct, Mode=TwoWay}" Placeholder="0.01 = 1%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="4" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="МЧС" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding McsPct, Mode=TwoWay}" Placeholder="0.01 = 1%"/>
                                </VerticalStackLayout>
                            </Border>

                            <!-- Row 2 -->
                            <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Отклонения" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding DeviationPct, Mode=TwoWay}" Placeholder="0.015 = 1.5%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="НДС" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding VatPct, Mode=TwoWay}" Placeholder="0.16 = 16%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="2" Style="{StaticResource CellBorder}" Padding="6">
                                <VerticalStackLayout Spacing="4">
                                    <Label Style="{StaticResource FieldLabel}" Text="Налог на прибыль" TextColor="Gray"/>
                                    <Entry Style="{StaticResource FieldEntry}" HorizontalTextAlignment="End" Text="{Binding ProfitTaxPct, Mode=TwoWay}" Placeholder="0.15 = 15%"/>
                                </VerticalStackLayout>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="3" Grid.ColumnSpan="2" Style="{StaticResource CellBorder}" Padding="6">
                                <Button Text="Пересчитать" Command="{Binding RecalculateAsyncCommand}" HorizontalOptions="Fill"/>
                            </Border>
                        </Grid>

                        <ScrollView Orientation="Both" HeightRequest="300" HorizontalScrollBarVisibility="Always" VerticalScrollBarVisibility="Always">
                            <Grid>
                                <!-- Purchase-only columns: #, Name, Qty, Base, Customs, Loading, Logistics, Warehouse, Declaration, Certification, MChs, Deviation, Cost/Unit, Line Total -->
                                <Grid RowDefinitions="Auto,*" ColumnDefinitions="40,240,90,110,110,110,110,110,110,110,110,110,120,140" ColumnSpacing="0">
                                    <Grid Grid.Row="0" ColumnSpacing="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="240"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="140"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="#"/></Border>
                                        <Border Grid.Column="1" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Наименование"/></Border>
                                        <Border Grid.Column="2" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Кол-во" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="3" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="База UZS" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="4" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Тамож/шт" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="5" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Погруз/шт" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="6" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Логистика" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="7" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Склад" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="8" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Декл" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="9" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Сертиф" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="10" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="МЧС" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="11" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Откл" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="12" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Себес/шт" HorizontalTextAlignment="End"/></Border>
                                        <Border Grid.Column="13" Style="{StaticResource CellBorder}"><Label Style="{StaticResource TableHeaderLabel}" Text="Итого (UZS)" HorizontalTextAlignment="End"/></Border>
                                    </Grid>
                                    <CollectionView Grid.Row="1" ItemsSource="{Binding Rows}" SelectionMode="None">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid ColumnSpacing="0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="40"/>
                                                        <ColumnDefinition Width="240"/>
                                                        <ColumnDefinition Width="90"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="140"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="•"/></Border>
                                                    <Border Grid.Column="1" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellLabel}" Text="{Binding SkuOrName}"/></Border>
                                                    <Border Grid.Column="2" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding Quantity, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="3" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding BasePriceUzs, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="4" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding CustomsUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="5" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding LoadingUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="6" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding LogisticsUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="7" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding WarehouseUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="8" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding DeclarationUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="9" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding CertificationUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="10" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding McsUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="11" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding DeviationUzsPerUnit, StringFormat='{0:N2}'}"/></Border>
                                                    <Border Grid.Column="12" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding CostPerUnitUzs, StringFormat='{0:N2}'}"/></Border>
                                                    <!-- Per-position total cost in UZS -->
                                                    <Border Grid.Column="13" Style="{StaticResource CellBorder}"><Label Style="{StaticResource CellNum}" Text="{Binding LineCostUzs, StringFormat='{0:N2}'}"/></Border>
                                                </Grid>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </Grid>
                            </Grid>
                        </ScrollView>

                        <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,6,0,0">
                            <Label Grid.Column="0" Text="Итого" FontSize="14"/>
                            <Label Grid.Column="1" Text="{Binding TotalQty, StringFormat='Кол-во: {0:N2}'}" FontSize="14"/>
                            <Label Grid.Column="2" Text="{Binding TotalBaseSumUzs, StringFormat='База: {0:N2} UZS'}" FontSize="14"/>
                        </Grid>
                    </VerticalStackLayout>
                </ContentView.Content>
            </ContentView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>