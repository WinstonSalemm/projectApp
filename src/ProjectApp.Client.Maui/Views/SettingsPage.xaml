<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ProjectApp.Client.Maui.ViewModels"
             x:Class="ProjectApp.Client.Maui.Views.SettingsPage"
             Title="РќР°СЃС‚СЂРѕР№РєРё">
  <ContentPage.Content>
    <ScrollView>
      <VerticalStackLayout Padding="16" Spacing="16">
        <Label Text="РџРѕРґРєР»СЋС‡РµРЅРёРµ" FontAttributes="Bold" FontSize="20"/>
        <Frame>
          <VerticalStackLayout Spacing="12">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
              <Label Text="РСЃРїРѕР»СЊР·РѕРІР°С‚СЊ API" VerticalOptions="Center"/>
              <Switch Grid.Column="1" IsToggled="{Binding UseApi, Mode=TwoWay}"/>
            </Grid>
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
              <Label Text="API Base URL" VerticalOptions="Center"/>
              <Entry Grid.Column="1" Placeholder="http://localhost:5028" Text="{Binding ApiBaseUrl, Mode=TwoWay}"/>
            </Grid>
          </VerticalStackLayout>
        </Frame>

        <Button Text="РЎРѕС…СЂР°РЅРёС‚СЊ" Command="{Binding SaveCommand}"/>
      </VerticalStackLayout>
    </ScrollView>
  </ContentPage.Content>
</ContentPage>


