<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ProjectApp.Client.Maui.Models"
             x:Class="ProjectApp.Client.Maui.Views.ClientCreatePage"
             Title="Новый клиент">
  <ScrollView>
    <VerticalStackLayout Padding="16" Spacing="12">
      <Picker Title="Тип клиента" SelectedItem="{Binding Type}">
        <Picker.ItemsSource>
          <x:Array Type="{x:Type models:ClientType}">
            <x:Static Member="models:ClientType.Individual" />
            <x:Static Member="models:ClientType.Company" />
          </x:Array>
        </Picker.ItemsSource>
      </Picker>

      <Entry Placeholder="Имя/Компания" Text="{Binding Name}"/>
      <Entry Placeholder="Телефон" Text="{Binding Phone}"/>
      <Entry Placeholder="ИНН (необязательно)" Text="{Binding Inn}"/>

      <Button Text="Сохранить" Command="{Binding SaveCommand}"/>
    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
