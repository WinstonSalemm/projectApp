<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:ProjectApp.Client.Maui.Behaviors"
             x:Class="ProjectApp.Client.Maui.Views.SuppliesPage"
             x:Name="Root"
             Title="Поставки (ND-40)">
  <ScrollView>
    <VerticalStackLayout Padding="12" Spacing="16">
      <!-- Supply section -->
      <Label Text="Новая поставка в ND-40" FontAttributes="Bold"/>

      <!-- Step 1: pick product -->
      <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
        <Button Text="Выбрать товар" Clicked="OnPickProductForSupplyClicked"/>
        <VerticalStackLayout Grid.Column="1" Spacing="2">
          <Label Text="{Binding NewProductSku}" FontAttributes="Bold"/>
          <Label Text="{Binding NewProductName}" FontSize="12" TextColor="Gray"/>
        </VerticalStackLayout>
        <Button Grid.Column="2" Text="Новый товар" Clicked="OnCreateProductClicked"/>
      </Grid>

      <!-- Step 2: qty/cost/code -->
      <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
        <VerticalStackLayout>
          <Label Text="Кол-во" FontSize="12"/>
          <Entry Placeholder="Напр. 10,5" Text="{Binding NewQty}" Keyboard="Numeric">
            <Entry.Behaviors>
              <behaviors:DecimalBehavior />
            </Entry.Behaviors>
          </Entry>
        </VerticalStackLayout>
        <VerticalStackLayout>
          <Label Text="Себестоимость" FontSize="12"/>
          <Entry Placeholder="0" Text="{Binding NewUnitCost}" Keyboard="Numeric">
            <Entry.Behaviors>
              <behaviors:DecimalBehavior />
            </Entry.Behaviors>
          </Entry>
        </VerticalStackLayout>
        <VerticalStackLayout>
          <Label Text="Код (партия)" FontSize="12"/>
          <Entry Placeholder="напр. A-001" Text="{Binding NewCode}"/>
        </VerticalStackLayout>
      </Grid>

      <HorizontalStackLayout Spacing="12">
        <Label Text="{Binding SelectedNd40Qty, StringFormat='ND-40: {0:N0}'}" FontSize="11"/>
        <Label Text="{Binding SelectedIm40Qty, StringFormat='IM-40: {0:N0}'}" FontSize="11"/>
        <Label Text="{Binding SelectedTotalQty, StringFormat='Всего: {0:N0}'}" FontSize="11"/>
        <Label Text="{Binding NewLineTotal, StringFormat='Сумма строки: {0:N0}'}" FontSize="11" FontAttributes="Bold"/>
      </HorizontalStackLayout>

      <Entry Placeholder="Примечание" Text="{Binding NewNote}"/>

      <HorizontalStackLayout Spacing="8">
        <Button Text="Добавить позицию" Command="{Binding AddItemCommand}"/>
        <Button Text="Создать поставку" Command="{Binding CreateSupplyCommand}"/>
      </HorizontalStackLayout>

      <!-- Items list -->
      <CollectionView ItemsSource="{Binding Items}">
        <CollectionView.Header>
          <Grid ColumnDefinitions="Auto,*,Auto,80,100,Auto,Auto" Padding="6" ColumnSpacing="8">
            <Label Text="SKU" FontAttributes="Bold"/>
            <Label Grid.Column="1" Text="Название" FontAttributes="Bold"/>
            <Label Grid.Column="2" Text="Код" FontAttributes="Bold"/>
            <Label Grid.Column="3" Text="Кол-во" FontAttributes="Bold" HorizontalTextAlignment="End"/>
            <Label Grid.Column="4" Text="Себестоим." FontAttributes="Bold" HorizontalTextAlignment="End"/>
            <Label Grid.Column="5" Text="Итого" FontAttributes="Bold" HorizontalTextAlignment="End"/>
            <Label Grid.Column="6" Text=""/>
          </Grid>
        </CollectionView.Header>
        <CollectionView.ItemTemplate>
          <DataTemplate>
            <Grid ColumnDefinitions="Auto,*,Auto,80,100,Auto,Auto" ColumnSpacing="8" Padding="6">
              <Label Text="{Binding Sku}" FontAttributes="Bold"/>
              <Label Grid.Column="1" Text="{Binding Name}" LineBreakMode="TailTruncation"/>
              <Label Grid.Column="2" Text="{Binding Code}"/>
              <Entry Grid.Column="3" Text="{Binding Qty, Mode=TwoWay}" Keyboard="Numeric" HorizontalTextAlignment="End" WidthRequest="80">
                <Entry.Behaviors>
                  <behaviors:DecimalBehavior />
                </Entry.Behaviors>
              </Entry>
              <Entry Grid.Column="4" Text="{Binding UnitCost, Mode=TwoWay}" Keyboard="Numeric" HorizontalTextAlignment="End" WidthRequest="100">
                <Entry.Behaviors>
                  <behaviors:DecimalBehavior />
                </Entry.Behaviors>
              </Entry>
              <Label Grid.Column="5" Text="{Binding Total, StringFormat='{0:N0}'}" FontAttributes="Bold" HorizontalTextAlignment="End"/>
              <Button Grid.Column="6" Text="Удалить" Command="{Binding Source={x:Reference Root}, Path=BindingContext.RemoveItemCommand}" CommandParameter="{Binding .}"/>
            </Grid>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>

      <BoxView HeightRequest="1" BackgroundColor="#ddd"/>

      <!-- Transfer section -->
      <Label Text="Перевод в IM-40 по коду" FontAttributes="Bold"/>
      <Entry Placeholder="Код партии" Text="{Binding TransferCode}"/>
      <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
        <Button Text="Выбрать товар" Clicked="OnPickProductForTransferClicked"/>
        <VerticalStackLayout Grid.Column="1" Spacing="2">
          <Label Text="{Binding TransferProductSku}" FontAttributes="Bold"/>
          <Label Text="{Binding TransferProductName}" FontSize="12" TextColor="Gray"/>
        </VerticalStackLayout>
        <Grid Grid.Column="2" ColumnDefinitions="*,Auto" ColumnSpacing="8">
          <Entry Grid.Column="0" Placeholder="Кол-во" Text="{Binding TransferQty}" Keyboard="Numeric">
            <Entry.Behaviors>
              <behaviors:DecimalBehavior />
            </Entry.Behaviors>
          </Entry>
          <Button Grid.Column="1" Text="Добавить" Command="{Binding AddTransferItemCommand}"/>
        </Grid>
      </Grid>

      <HorizontalStackLayout Spacing="12">
        <Label Text="{Binding TransferNd40Qty, StringFormat='ND-40: {0:N0}'}" FontSize="11"/>
        <Label Text="{Binding TransferIm40Qty, StringFormat='IM-40: {0:N0}'}" FontSize="11"/>
        <Label Text="{Binding TransferTotalQty, StringFormat='Всего: {0:N0}'}" FontSize="11"/>
      </HorizontalStackLayout>

      <CollectionView ItemsSource="{Binding TransferItems}">
        <CollectionView.Header>
          <Grid ColumnDefinitions="Auto,80,Auto" Padding="6" ColumnSpacing="8">
            <Label Text="ProductId" FontAttributes="Bold"/>
            <Label Grid.Column="1" Text="Кол-во" FontAttributes="Bold" HorizontalTextAlignment="End"/>
            <Label Grid.Column="2" Text=""/>
          </Grid>
        </CollectionView.Header>
        <CollectionView.ItemTemplate>
          <DataTemplate>
            <Grid ColumnDefinitions="Auto,80,Auto" ColumnSpacing="8" Padding="6">
              <Label Text="{Binding ProductId}"/>
              <Entry Grid.Column="1" Text="{Binding Qty, Mode=TwoWay}" Keyboard="Numeric" HorizontalTextAlignment="End" WidthRequest="80">
                <Entry.Behaviors>
                  <behaviors:DecimalBehavior />
                </Entry.Behaviors>
              </Entry>
              <Button Grid.Column="2" Text="Удалить" Command="{Binding Source={x:Reference Root}, Path=BindingContext.RemoveTransferItemCommand}" CommandParameter="{Binding .}"/>
            </Grid>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>

      <Button Text="Перевести в IM-40" Command="{Binding TransferCommand}"/>

      <Label Text="{Binding StatusMessage}" TextColor="Red"/>
    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
