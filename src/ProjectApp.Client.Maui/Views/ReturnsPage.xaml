<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ProjectApp.Client.Maui.ViewModels"
             x:Class="ProjectApp.Client.Maui.Views.ReturnsPage"
             x:Name="Root"
             Title="Р’РѕР·РІСЂР°С‚" x:DataType="viewModels:ReturnsViewModel">
  <Grid Padding="24" RowDefinitions="Auto,Auto,Auto,Auto,*,Auto,Auto" RowSpacing="16">
    <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
      <Label Text="РџСЂРѕРґР°Р¶Р° #" VerticalTextAlignment="Center"/>
      <Entry Grid.Column="1" Keyboard="Numeric" Text="{Binding RefSaleId}"/>
    </Grid>
    <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
      <Label Text="РљР»РёРµРЅС‚Id" VerticalTextAlignment="Center"/>
      <Entry Grid.Column="1" Keyboard="Numeric" Text="{Binding ClientId}"/>
    </Grid>
    <Entry Grid.Row="2" Placeholder="РџСЂРёС‡РёРЅР° (РЅРµРѕР±СЏР·Р°С‚РµР»СЊРЅРѕ)" Text="{Binding Reason}"/>

    <Label Grid.Row="3" Text="РџРѕР·РёС†РёРё (РѕСЃС‚Р°РІСЊС‚Рµ РїСѓСЃС‚Рѕ РґР»СЏ РїРѕР»РЅРѕРіРѕ РІРѕР·РІСЂР°С‚Р°)" Style="{StaticResource SectionTitle}"/>
    <Grid Grid.Row="3" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="12" Margin="0,12,0,0">
      <Entry Placeholder="SaleItemId" Keyboard="Numeric" Text="{Binding NewSaleItemId}"/>
      <Entry Grid.Column="1" Placeholder="Qty" Keyboard="Numeric" Text="{Binding NewQty}"/>
      <Button Grid.Column="2" Text="Р”РѕР±Р°РІРёС‚СЊ" Command="{Binding AddItemCommand}" Style="{StaticResource OutlinedButton}"/>
    </Grid>

    <CollectionView Grid.Row="4" ItemsSource="{Binding Items}">
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <Frame Style="{StaticResource CardFrame}">
            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="12">
              <Label Text="{Binding SaleItemId}"/>
              <Label Grid.Column="1" Text="{Binding Qty}"/>
              <Button Grid.Column="2" Text="РЈРґР°Р»РёС‚СЊ" Command="{Binding Source={x:Reference Root}, Path=BindingContext.RemoveItemCommand}" CommandParameter="{Binding .}" Style="{StaticResource OutlinedButton}"/>
            </Grid>
          </Frame>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>

    <Button Grid.Row="5" Text="РЎРѕР·РґР°С‚СЊ РІРѕР·РІСЂР°С‚" Command="{Binding CreateReturnCommand}" Style="{StaticResource CtaPrimary}"/>

    <Label Grid.Row="6" Text="{Binding StatusMessage}" TextColor="Red"/>
  </Grid>
</ContentPage>

