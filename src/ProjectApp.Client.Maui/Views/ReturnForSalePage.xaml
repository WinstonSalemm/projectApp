<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ProjectApp.Client.Maui.ViewModels"
             x:Class="ProjectApp.Client.Maui.Views.ReturnForSalePage"
             Title="Р’РѕР·РІСЂР°С‚ РїРѕ РїСЂРѕРґР°Р¶Рµ" x:DataType="viewModels:ReturnForSaleViewModel">
  <Grid Padding="16" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="12">
    <VerticalStackLayout Grid.Row="0" Spacing="6">
      <Label Text="РРЅС„РѕСЂРјР°С†РёСЏ Рѕ РїСЂРѕРґР°Р¶Рµ" FontAttributes="Bold"/>
      <Label Text="{Binding SaleId, StringFormat='РџСЂРѕРґР°Р¶Р° #{0}'}"/>
      <Label Text="{Binding ClientName}"/>
      <Label Text="{Binding PaymentType, Converter={StaticResource PaymentTypeToRuConverter}}"/>
      <Label Text="{Binding CreatedAt, Converter={StaticResource DateTimeToRuConverter}}"/>
    </VerticalStackLayout>

    <CollectionView Grid.Row="1" ItemsSource="{Binding Lines}">
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <Frame Padding="8" HasShadow="False" CornerRadius="8">
            <Grid ColumnDefinitions="*,*,Auto" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="8">
              <Label Grid.Row="0" Grid.Column="0" Text="{Binding Sku}" FontAttributes="Bold"/>
              <Label Grid.Row="0" Grid.Column="1" Text="{Binding Name}"/>
              <Label Grid.Row="1" Grid.Column="0" Text="РџСЂРѕРґР°РЅРѕ:"/>
              <Label Grid.Row="1" Grid.Column="1" Text="{Binding SoldQty}"/>
              <Label Grid.Row="2" Grid.Column="0" Text="Р”РѕСЃС‚СѓРїРЅРѕ Рє РІРѕР·РІСЂР°С‚Сѓ:"/>
              <Label Grid.Row="2" Grid.Column="1" Text="{Binding AvailableQty}"/>
              <Entry Grid.Row="0" Grid.Column="2" WidthRequest="100" Keyboard="Numeric" Text="{Binding ReturnQty}"/>
            </Grid>
          </Frame>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>

    <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="8">
      <Button Text="Р’С‹Р±СЂР°С‚СЊ РІСЃС‘" Command="{Binding SelectAllCommand}"/>
      <Button Grid.Column="1" Text="{Binding ActionButtonText}" Command="{Binding SubmitCommand}"/>
    </Grid>

    <Label Grid.Row="3" Text="{Binding StatusMessage}" TextColor="Red"/>
  </Grid>
</ContentPage>

