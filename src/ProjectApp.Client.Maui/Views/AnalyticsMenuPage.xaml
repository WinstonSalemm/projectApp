<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectApp.Client.Maui.Views.AnalyticsMenuPage"
             Title="Аналитика"
             BackgroundColor="{DynamicResource Color.Background}">
  
  <ScrollView Padding="24" VerticalScrollBarVisibility="Always">
    <VerticalStackLayout Spacing="20">
      <Label Text="📊 Аналитика" FontSize="32" FontAttributes="Bold" Margin="0,8,0,32"/>
      
      <!-- Финансовая аналитика -->
      <Frame x:Name="FinanceCard" 
             Style="{StaticResource Card}"
             Padding="24">
        <Frame.GestureRecognizers>
          <TapGestureRecognizer Tapped="OnFinancesTapped"/>
          <PointerGestureRecognizer PointerEntered="OnCardPointerEntered" PointerExited="OnCardPointerExited"/>
        </Frame.GestureRecognizers>
        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="20">
          <Border Grid.Column="0" 
                  WidthRequest="64" 
                  HeightRequest="64" 
                  BackgroundColor="{DynamicResource Color.Surface.Muted}" 
                  StrokeThickness="0" 
                  StrokeShape="RoundRectangle 12"
                  VerticalOptions="Center">
            <Label Text="💰" FontSize="36" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
          </Border>
          <VerticalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
            <Label Text="Финансовая аналитика" FontSize="20" FontAttributes="Bold" TextColor="{DynamicResource Color.Text.Primary}"/>
            <Label Text="Выручка, прибыль, маржинальность" FontSize="14" TextColor="{DynamicResource Color.Text.Secondary}"/>
          </VerticalStackLayout>
          <Label Grid.Column="2" Text="›" FontSize="40" TextColor="{DynamicResource Color.Text.Secondary}" VerticalOptions="Center"/>
        </Grid>
      </Frame>
      
      <!-- Аналитика по менеджерам -->
      <Frame x:Name="ManagersCard"
             Style="{StaticResource Card}"
             Padding="24">
        <Frame.GestureRecognizers>
          <TapGestureRecognizer Tapped="OnManagersTapped"/>
          <PointerGestureRecognizer PointerEntered="OnCardPointerEntered" PointerExited="OnCardPointerExited"/>
        </Frame.GestureRecognizers>
        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="20">
          <Border Grid.Column="0" 
                  WidthRequest="64" 
                  HeightRequest="64" 
                  BackgroundColor="{DynamicResource Color.Surface.Muted}" 
                  StrokeThickness="0" 
                  StrokeShape="RoundRectangle 12"
                  VerticalOptions="Center">
            <Label Text="👥" FontSize="36" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
          </Border>
          <VerticalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
            <Label Text="Аналитика по менеджерам" FontSize="20" FontAttributes="Bold" TextColor="{DynamicResource Color.Text.Primary}"/>
            <Label Text="Обороты, клиенты, график продаж" FontSize="14" TextColor="{DynamicResource Color.Text.Secondary}"/>
          </VerticalStackLayout>
          <Label Grid.Column="2" Text="›" FontSize="40" TextColor="{DynamicResource Color.Text.Secondary}" VerticalOptions="Center"/>
        </Grid>
      </Frame>
      
      <!-- Управление себестоимостью -->
      <Frame x:Name="ProductsCard"
             Style="{StaticResource Card}"
             Padding="24">
        <Frame.GestureRecognizers>
          <TapGestureRecognizer Tapped="OnProductCostsTapped"/>
          <PointerGestureRecognizer PointerEntered="OnCardPointerEntered" PointerExited="OnCardPointerExited"/>
        </Frame.GestureRecognizers>
        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="20">
          <Border Grid.Column="0" 
                  WidthRequest="64" 
                  HeightRequest="64" 
                  BackgroundColor="{DynamicResource Color.Surface.Muted}" 
                  StrokeThickness="0" 
                  StrokeShape="RoundRectangle 12"
                  VerticalOptions="Center">
            <Label Text="📦" FontSize="36" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
          </Border>
          <VerticalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
            <Label Text="Управление себестоимостью" FontSize="20" FontAttributes="Bold" TextColor="{DynamicResource Color.Text.Primary}"/>
            <Label Text="Себестоимость товаров, маржа" FontSize="14" TextColor="{DynamicResource Color.Text.Secondary}"/>
          </VerticalStackLayout>
          <Label Grid.Column="2" Text="›" FontSize="40" TextColor="{DynamicResource Color.Text.Secondary}" VerticalOptions="Center"/>
        </Grid>
      </Frame>
      
    </VerticalStackLayout>
  </ScrollView>
</ContentPage>
